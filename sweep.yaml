program: train.py
method: bayes
metric:
  goal: minimize
  name: "valid_rmse"
parameters:
  virtual_batch_size:
    max: 1024
    min: 64
    distribution: int_uniform
  max_epochs:
    value: 1000
  batch_size:
    max: 5048
    min: 128
    distribution: int_uniform
  patience:
    max: 20
    min: 10
    distribution: int_uniform
  n_steps:
    max: 16
    min: 2
    distribution: int_uniform
  factor:
    max: 0.5
    min: 0.02
    distribution: uniform
  n_d:
    max: 256
    min: 12
    distribution: int_uniform
  n_a:
    max: 256
    min: 12
    distribution: int_uniform
  lr:
    max: 0.05
    min: 0.0025
    distribution: uniform
